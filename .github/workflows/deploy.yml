name: Deploy PYTHIA

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted  # Le runner tourne sur le Jetson lui-même
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          docker compose up --build -d
          docker compose ps
